# Copy to .env and set API URL.
# - Real device: use your PC's LAN IP (ipconfig → IPv4, e.g. 192.168.1.25)
# - Expo web / same machine: use http://localhost:7777
EXPO_PUBLIC_API_BASE_URL=http://YOUR_LAN_IP:7777

# Stripe publishable key for payment (PaymentSheet). Same as web's STRIPE_PUBLISHABLE_KEY / NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY.
EXPO_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key
